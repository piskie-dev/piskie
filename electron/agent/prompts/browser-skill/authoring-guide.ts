export const BROWSER_SKILL_AUTHORING_GUIDE = `## Browser Skill 构建原则

把使用 browser 探索出的真实网站操作提炼为 executable Skill。Assignment 定义待固化的能力范围，不是一次性业务执行指令；即使范围已经明确，也必须先深入探索目标流程、公共依赖、失败点和人工边界。默认产物只有 SKILL.md 与 skill.ts；只有确有按需知识时才增加 references。

SKILL.md 是给未来 AI 的简洁网站操作指南。Skill 目录名、frontmatter name 和 skill.ts 的 defineSkill name 使用同一个小写连字符名称；frontmatter 只写 name、type: browser 和 description。description 写清“做什么 + 匹配什么网站/意图 + 何时使用”。正文只说明未来使用时需要的 Skill-first、函数组合、能力缺口、人工边界和 browser 恢复条件，不记录构建过程、验收分工或测试历史。函数名、description 和参数签名来自 skill.ts 并由 load_skill 自动渲染，不在正文复制完整 Schema。

skill.ts 使用 defineSkill({ name, domain: 'browser', functions })。它写网站业务编排，不写浏览器驱动：params 定义未来 AI 传入的业务输入，run 用当前 Browser Skill SDK Reference 中的 ctx.browser 能力组合平台原子能力，locator、操作顺序、页面判断和结果解析来自真实探索，ok/fail 返回业务结果或恢复事实。每个函数只暴露业务参数；browserId、taskId、agentId、callId 等宿主标识不得作为函数参数。成功时在模型可见 text 中返回完整、结构化且字段稳定的业务结果，通常序列化为 JSON；data 只可作同源诊断副本，不能藏放 text 缺失的关键结果。失败返回普通 ok=false，并说明失败阶段、页面事实和恢复方向。

**输出完整性**：已批准验收要求中每个场景的必需字段都必须由对应函数返回。业务结果必须是按业务语义命名的结构化对象；列表必须返回字段一致的对象数组。列表和详情应返回具有独立业务意义的字段；不得把价格、时间、地点、状态、标题等不同语义只揉成一个 summary 或其他文本字段。同一列表的每个项保持同一稳定字段结构。不得把页面 HTML、DOM、snapshot、整页文本、locator 或 UID 作为业务结果，也不得将这些内容放入 data 充当结构化结果；它们只可用于构建期观察，失败诊断只返回定位问题所需的页面事实。真实成功样例中必需字段不得为空，不得使用“未知”、“N/A”、示例 ID 等占位值伪造完整。页面存在下游重新定位或调用所需的业务 ID、详情 URL、selectionKey 或同类稳定值时必须返回；页面确实不提供时如实说明，不猜造。函数 description 列出关键返回字段，SKILL.md 说明函数间如何传递它们，实际 text 必须使用同名且语义一致的字段。

分清两层函数：对外业务函数是在本次固化范围内可独立使用的完整网站业务工具；底层浏览器函数由当前 Browser Skill SDK API Reference 固定提供。只可调用 Reference 中存在的方法，不得发明方法、自建 Puppeteer/CDP 连接、访问 BrowserManager、使用任意 Node/第三方 import 或自行选择浏览器。

先结合已批准的业务工具清单和完整网站流程确定公开函数边界。计划中的工具拆解规定业务粒度，不代表参数、返回字段和实现已经正确；这些必须根据真实探索确定。如果计划中的某个工具实际只完成页面准备，结果只能供唯一下一步继续使用，就在写源码前回报证据和合并后的工具方案，不要把它实现成公开函数。一个搜索工具应在同一公开函数内完成条件填写、提交、等待和结果读取，直接返回搜索结果；“已填写”“已配置”“可以提交”只属于函数内部状态。只有当前结果本身可以独立使用，或未来 AI 必须依据该结果选择下一项业务操作时，才形成两个公开函数。

从已确认的真实入口开始，通过页面实际操作到达目标状态。只可导航至已确认的站点入口，或当前页面、上游业务函数真实返回且属于目标站点的 URL；不得拼造搜索、列表、详情或终态 URL，也不得用直接跳转替代本函数承诺的填写、选择、点击等页面步骤。

browser snapshot 只用于构建期观察当前页面；其中的 UID、节点层级和 StaticText 等可访问性节点类型不构成固化 locator。根据观察到的业务目标，在当前 DOM 中确认实际 Element，并使用每次调用重新解析的标准 HTML/ARIA 语义、站点维护的 data-*、id、name、业务文本和稳定容器关系。操作前确认目标唯一、可见且实际命中，操作后验证业务状态；不得把 UID、snapshot 节点或 ElementHandle 写入 skill.ts、业务参数或返回值。上下游传递站点业务 ID、详情 URL 或可重新定位的 selectionKey。只能得到临时 UID 且当前 SDK 无法稳定表达时，回报 platform capability gap，不要生成伪稳定代码或自造 UID 刷新旁路。

browser 轨迹只作证据，不直接序列化。把完成一个业务工具所需的页面操作、判断、结果解析、条件分支、必要重试和私有 helper 写入同一个公开函数；click、fill 等原子动作的数量、经过的页面数量、源码长度、失败恢复和测试方便程度都不决定公开函数边界。若当前实现只返回供固定下一步使用的中间状态，先与下一步合并，不要为了缩短函数或方便定位错误而增加公开函数。Reference 没有完成目标所需的原子能力时，明确回报 platform capability gap，不要猜测或旁路实现。

每完成或修改一个具有完整业务意义的公开函数，就立即调用 browser_skill_build。成功后先 load_skill 检查未来 AI 看到的教学，再恢复到合理入口并用 skill_call 真实调用；不得为了提前 build 或增量测试，把尚未产生业务结果的中间步骤暴露为公开函数。失败则修改、重建、重测。当次 UID 点击成功不等于已经固化成功。测试不只检查页面到达：还要检查必需字段存在、非空、语义正确，且一个工具返回的真实业务结果能用于需要该结果的后续工具。参数取值、页面状态或结果形态会进入不同业务分支时，必须分别使用真实差异样例执行 skill_call，核对页面终态、结构化结果和 locator 在对应状态中的有效性；单个成功样例不能代表其他分支通过。验证会改变页面状态的分支时，必须使用与当前页面真实值不同的输入触发操作；若实现会在页面已满足请求时跳过操作，至少一个测试必须从不同实时值开始，实际完成改变并验证业务结果。重新导航到同一入口不能证明网站已清除持久化状态。浏览器动作或读取失败不得被吞成 notDisplayed、空结果或被忽略的分支；只有页面事实确认目标不存在时，才能报告缺失。retryable 只有在无需新增人工操作、使用相同输入再次调用有合理成功机会时才为 true；输入错误、业务拒绝和实现不变量失败必须给出与事实相符的恢复方向，不能盲目重试。同一 locator 或私有 helper 被不同业务分支、状态型控件或连续调用复用时，必须交错建立不同页面状态验证；出现错误命中、意外导航、依赖隐藏元素或只在干净入口偶然成功，均说明该交互尚未固化。发现一类 locator 或等待问题后，检查同一 Skill 中使用相同实现的全部位置，不能只修当前失败样例。源码修改并重新 build 后，先判断修改影响的公开函数、共享 helper、locator、等待条件、参数与返回结构以及上下游串联路径。所有直接或间接受影响的路径必须基于最新构建重新实测；能够确认未经过受改代码且输入输出语义未变化的既有验证可以保留，不为形式重复执行。已有成功只有在确实执行过待验证分支时才构成证据；当前页面状态恰好满足请求、导致关键操作被跳过的调用，不能证明该操作分支有效。全部公开函数逐个通过后，再按 SKILL.md 组合自验已批准验收要求的全部场景。最终标准是未来 AI 能用少量完整业务工具完成目标，不是文件写完、TypeScript 编译通过或每个页面阶段都有对应函数。`;

export function renderBrowserSkillAuthoringGuide(guide = BROWSER_SKILL_AUTHORING_GUIDE): string {
  if (!guide.trim()) {
    throw new Error('Browser Skill Authoring Guide is unavailable');
  }
  return guide;
}
