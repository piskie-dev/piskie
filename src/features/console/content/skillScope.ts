import type { SkillScope } from '../../../../shared/types/skill';

export const SKILL_SCOPE_KEYS: Record<SkillScope, string> = {
  project: 'sessionWorkbenchUi.composer.skills.project',
  user: 'sessionWorkbenchUi.composer.skills.user',
  builtin: 'sessionWorkbenchUi.composer.skills.builtin',
};
